# ── API + Socket URL ──────────────────────────────────────────────────────────
# Local dev — Vite proxy handles /api so these aren't needed locally.
# Set these for production (your Railway/Render server URL):
VITE_API_URL=https://your-server.railway.app
VITE_SOCKET_URL=https://your-server.railway.app
